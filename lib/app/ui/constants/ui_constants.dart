import 'package:flutter/material.dart';

abstract class UiConstants {
  static const appBarPadding = EdgeInsets.symmetric(horizontal: 12, vertical: 6);
  static const horizontalPadding = EdgeInsets.symmetric(horizontal: 20);
  static const bigHorizontalPadding = EdgeInsets.symmetric(horizontal: 45);
  static const basePadding = EdgeInsets.symmetric(horizontal: 20, vertical: 14);
  static const baseAllPadding = EdgeInsets.all(16);
  static const bigVerticalPadding = EdgeInsets.symmetric(vertical: 34);
}
